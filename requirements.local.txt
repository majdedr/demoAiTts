# FastAPI core
fastapi>=0.103.0
uvicorn[standard]>=0.23.2

# Pydantic models and validation
pydantic>=2.3.0

# Logging & structured output
loguru>=0.7.0

# AI / LLM integration
ctransformers>=0.4.5       # For loading TinyLlama GGUF model locally
diskcache>=5.6.3           # Optional caching, used in vector_store retrieval

# Data / vector search (RAG)
numpy>=1.24.0
scikit-learn>=1.3.2        # For embeddings or vector computations

# TTS / STT
sounddevice>=0.4.6         # For microphone audio capture
soundfile>=0.12.1          # Audio file reading/writing
pyttsx3>=2.90              # Offline TTS engine (Windows friendly)

# Other helpers
python-dotenv>=1.0.0       # Load environment variables
typing-extensions>=4.5.0   # Type hints compatibility

# Optional: Jinja2 if prompts or templates are used
jinja2>=3.1.6
MarkupSafe>=3.0.3
